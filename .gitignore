node_modules/
dist/
.env
.env.local

# Astro build cache and output (deployed via Pages artifact, not committed)
site/.astro/
site/dist/

# Admin build output (Vite outputs to site/admin/)
site/admin/

# Admin build output goes into site/public/admin/ — commit placeholder only
site/public/admin/
!site/public/admin/.gitkeep

# Generated state files — overwritten on each build, commit only placeholders
# (placeholder files are committed so Astro doesn't 404 on first build)
site/public/generated/state/content/
site/public/generated/build-manifest.json
site/public/generated/styles/
site/public/generated/js/

# Projector build output (generated at build time, not committed)
site/generated/

# TypeScript compile output for projector
tools/projector/dist/

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
